package com.system.sunday_management;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
// this annotation adds all the declaration add configuration auto configuration
public class SundayManagementApplication {

    public static void main(String[] args) {
        SpringApplication.run(SundayManagementApplication.class, args);
    }

}
